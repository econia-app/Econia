/**
 * Footer Econia — alias vers le footer canonique de la landing.
 *
 * Ce fichier existait avec son propre design (Syne/Space Grotesk non
 * chargées, liens sociaux placeholder). Il est maintenant un simple
 * re-export pour éviter les doublons et garantir la cohérence visuelle.
 *
 * Tout nouveau footer doit être édité dans `landing/LandingFooter.tsx`.
 */
export { default } from "./landing/LandingFooter";
